// types.ts
export interface ScoreboardData {
    player1: string;
    player2: string;
    score1: [number, number, number];
    score2: [number, number, number];
    currentGamePoints1: number;
    currentGamePoints2: number;
  }
  